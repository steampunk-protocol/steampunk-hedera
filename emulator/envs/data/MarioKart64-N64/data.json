{
  "info": {},
  "_notes": {
    "status": "EMPTY — RAM addresses TBD. The original addresses (0xF9xxxx) were out of N64 RDRAM range (max 8MB = 0x800000).",
    "source": "n64decomp/mk64 Player struct: pos@0x14(Vec3f), currentRank@0x04(s16), lapCount@0x08(s16), currentSpeed@0x9C(f32). Struct size=0xDD8. gPlayers[] base address unknown — needs runtime discovery.",
    "approach": "Boot ROM with empty info, use pixel_detect.py for lap/finish detection, or scan RAM for known patterns to find gPlayers base address."
  }
}
